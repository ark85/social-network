spring.application.name=network

spring.datasource.url=${NETWORK_DB_URL}
spring.datasource.username=${NETWORK_DB_USERNAME}
spring.datasource.password=${NETWORK_DB_PASSWORD}

jwt.secret=${JWT_SECRET}
jwt.access-token-ttl=${JWT_ACCESS_TOKEN_TTL}
